// TODO: Possibly change for input sanitization
export interface ISettingItem {
    contacts: boolean;
    notifications: boolean;
    whitelist: boolean;
    blacklist: boolean;
    block_calls: boolean;
    theme: string;
}